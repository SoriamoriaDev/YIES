<script type="text/javascript" src="/jquery/jqmeter.min.js"></script>
<script type="text/javascript" src="/js/progressbar.js"></script>
<script src="/js/blob-stream.js"></script>
<script src="/js/pdfkit.js"></script>
<script src="/js/pdfkit.js.map"></script>
<script src="/js/download.js"></script>
<script src="/js/base64pics.js"></script>

<script>
// create a PDF document and pipe to a blob
var doc = new PDFDocument({
    margins : {
        top: 50,
        bottom: 50,
        left: 100,
        right: 50
    },
    info: {
        Title: 'Report',
        Author: 'YIES - Insightful talent evaluation', // the name of the author
        Subject: '', // the subject of the document
        Keywords: '', // keywords associated with the document
        CreationDate: 'DD/MM/YYYY' // the date the document was created (added automatically by PDFKit)
}
});

var yTitle = 50;
var yInfo = 110;
var yStrength = 270;
var yWeaknesses = 470;
var yFooter = 730;
var yCompetences = 150;

var stream = doc.pipe(blobStream());

</script>

<div class="container-800">

    <h1>{{ title }} {{#each info}} - {{[6]}}</h1>

<script>
/*
//**---------------------------------------RULERS----------------------------**
// ** TOP RULER 0 to 520 **
    //Text for test
    doc.fontSize(10)
            .fillColor('grey')
            .text('0', 0, 0);

    //Text for test
    doc.fontSize(10)
            .fillColor('grey')
            .text('20', 20, 0);

    //Text for test
    doc.fontSize(10)
            .fillColor('grey')
            .text('100', 100, 0);

    //Text for test
    doc.fontSize(10)
            .fillColor('grey')
            .text('200', 200, 0);

    //Text for test
    doc.fontSize(10)
            .fillColor('grey')
            .text('300', 300, 0);

    //Text for test
    doc.fontSize(10)
            .fillColor('grey')
            .text('400', 400, 0);

    //Text for test
    doc.fontSize(10)
            .fillColor('grey')
            .text('500', 500, 0);

    //Text for test
    doc.fontSize(10)
            .fillColor('grey')
            .text('|', 510, 0, {align: "right"});

// ** LEFT RULER 0 to 520 **
//Text for test
doc.fontSize(10)
        .fillColor('grey')
        .text('20', 50, 20);
doc.fontSize(10)
        .fillColor('grey')
        .text('50', 50, 50);
doc.fontSize(10)
        .fillColor('grey')
        .text('100', 50, 100);
doc.fontSize(10)
        .fillColor('grey')
        .text('150', 50, 150);
doc.fontSize(10)
        .fillColor('grey')
        .text('200', 50, 200);
doc.fontSize(10)
        .fillColor('grey')
        .text('250', 50, 250);
doc.fontSize(10)
        .fillColor('grey')
        .text('300', 50, 300);
doc.fontSize(10)
        .fillColor('grey')
        .text('350', 50, 350);
doc.fontSize(10)
        .fillColor('grey')
        .text('400', 50, 400);
doc.fontSize(10)
        .fillColor('grey')
        .text('450', 50, 450);
doc.fontSize(10)
        .fillColor('grey')
        .text('500', 50, 500);
doc.fontSize(10)
        .fillColor('grey')
        .text('550', 50, 550);
doc.fontSize(10)
        .fillColor('grey')
        .text('600', 50, 600);
doc.fontSize(10)
        .fillColor('grey')
        .text('650', 50, 650);
doc.fontSize(10)
        .fillColor('grey')
        .text('700', 50, 700);
doc.fontSize(10)
        .fillColor('grey')
        .text('720', 50, 720);

//**---------------------------------------RULERS----------------------------**
*/

//YIES logo
doc.image(yiesLogo, 95, yTitle,  {height: 20});

// Title report
doc.fontSize(25)
        .fillColor('grey')
        .text('Report overview', 205, yTitle+1);


</script>

    <div class="row">

        <div class="col-md-2">
            <img src="/img/Profile_avatar2.png" style="width:110px; height:110px;">
        </div>

        <div class="col-md-7">
            <p>Name: {{[1]}} {{[2]}}</p>
            <p>Position: {{[3]}}</p>
            <p>Department: {{[5]}}</p>
            <p>Company: {{[4]}}</p>
        </div>

<script>

doc.image(profilePic, 100, yInfo);

doc.fontSize(10)
        .text('Name: {{[1]}} {{[2]}}', 190, yInfo+3);
doc.fontSize(10)
        .text('Position: {{[3]}}', 190, yInfo+23);
doc.fontSize(10)
        .text('Department: {{[5]}}', 190, yInfo+43);
doc.fontSize(10)
        .text('Company: {{[4]}}', 190, yInfo+63);

doc.fontSize(15)
        .fillColor('grey')
        .text('"{{[6]}}"', 105, yInfo+110, {align: 'center'});


</script>
        <div class="col-md-3" align="right">
            <a class='action-icons' id="export-button"><i class="fa fa-file-pdf-o"></i>Export to PDF</a>
        </div>
    </div>



{{/each}}
    </br>


<script>

// Strength box
doc.lineJoin('round')
        .rect(100, yStrength, 450, 160)
        .lineWidth(1)
        .strokeColor("silver")
        .stroke();

//Strength title
doc.fontSize(11)
        .fillColor('grey')
        .text('STRENGTH', 110, yStrength+10);

</script>

    <article class="container-survey">

        <div class="row-heading">
            <div class="col-md-4">STRENGTH</div>
            <div class="col-md-8"></div>

        </div>
        <div class="row">
    {{#each strong}}
            <div class="col-md-4">
                <div class="container-circle" id="container{{@index}}"></div>
                <div class="row"><div align="center">{{[0]}}</div></div>

            </div>
<script>
        var bar = new ProgressBar.Circle(container{{@index}}, {
        //color: '#3cb878',
        // This has to be the same size as the maximum width to
        // prevent clipping
        strokeWidth: 7,
        trailWidth: 1,
        easing: 'easeInOut',
        duration: 3000,
        text: {
        autoStyleContainer: false
        },
        from: { color: '#aaa', width: 1 },
        to: { color: '##3cb878', width: 7 },
        // Set default step function for all animate calls
        step: function(state, circle) {
        circle.path.setAttribute('stroke', state.color);
        circle.path.setAttribute('stroke-width', state.width);

        var value = (circle.value() * 10).toFixed(1);
        if (value === 0) {
        circle.setText('');
        } else {
        circle.setText(value);
        }

        }
        });
        //bar.text.style.fontFamily = '"Raleway", Helvetica, sans-serif';
        bar.text.style.fontSize = '3rem';

        bar.animate(({{[1]}})/100);

</script>

<script>

//** STRENGTH **//

    //Green circle for each Strength
doc.circle(180+{{@index}}*150, yStrength+75, 0.4*{{[1]}})
        .lineWidth(7)
        .strokeColor("#3cb878")
        .stroke();

    //Score for each Strength Circle
doc.fontSize(20)
        .fillColor('grey')
        .text('{{[2]}}', 166+{{@index}}*150, yStrength+66);

//Text for each Strength
doc.fontSize(10)
        .fillColor('grey')
        .text('{{[0]}}', 180+{{@index}}*150-{{[0].length}}*2.3, yStrength+130);

</script>

    {{/each}}

        </div>

    </article>

<script>

    // Weakness box
    doc.lineJoin('round')
            .rect(100, yWeaknesses, 450, 160)
            .lineWidth(1)
            .strokeColor('silver')
            .stroke();

    //Weakness title
    doc.fontSize(11)
            .fillColor('grey')
            .text('AREAS FOR IMPROVEMENTS', 110, yWeaknesses+10);

</script>

        </br>
    <article class="container-survey">

        <div class="row-heading">
            <div class="col-md-4">AREAS FOR IMPROVEMENTS</div>
            <div class="col-md-8"></div>
        </div>
        {{#each weak}}
        <div class="row">
            <div class="col-md-4">{{[0]}}</div>
            <div class="col-md-6"><div id="weak{{@index}}"></div></div>
            <div class="col-md-2"><div align="center">{{[2]}}</div></div>
        </div>

        <script>
            $('#weak{{@index}}').jQMeter({
                goal: '100',
                raised: '{{[1]}}',
                width: "350px",
                height: "12px",
                bgColor: "#d7d7d7",
                barColor: "#f800a9",
                meterOrientation: "horizontal",
                animationSpeed: 3000,
                counterSpeed: 3000,
                displayTotal: false
            });

        </script>


<script>

    //** BARS FOR WEAKNESS **//

    //Text for each competence
    doc.fontSize(10)
            .fillColor('grey')
            .text('{{[0]}}', 110, yWeaknesses+50+{{@index}}*35);

    //Grey bg bar
    doc.lineCap('round')
            .moveTo(275, yWeaknesses+54+{{@index}}*35)
            .lineTo(475, yWeaknesses+54+{{@index}}*35)
            .lineWidth(8)
            .fillAndStroke("#d7d7d7")
            .stroke();

    //Pink bar for weakness level
    doc.lineCap('round')
            .moveTo(275, yWeaknesses+54+{{@index}}*35)
            .lineTo(275+{{[1]}}*2, yWeaknesses+54+{{@index}}*35)
            .lineWidth(8)
            .fillAndStroke("#f800a9")
            .stroke();

    //Text for average score
    doc.fontSize(10)
            .fillColor('grey')
            .text('{{[2]}}', 510, yWeaknesses+50+{{@index}}*35);


</script>



        {{/each}}

    </article>

<script>

doc.fontSize(10)
        .fillColor('blue')
        .text('WWW.YIES.CO', 100, yFooter, {align : 'center'})
        .link(300, yFooter, 70, 20, 'http://www.yies.co/');


doc.fontSize(10)
        .fillColor('grey')
        .text('1 / 2', 530, yFooter);

doc.addPage();

//YIES logo
doc.image(yiesLogo, 95, yTitle,  {height: 20});

// Title report
doc.fontSize(25)
        .fillColor('grey')
        .text('All competences in detail', 205, yTitle+1);

// All competences box
    doc.lineJoin('round')
            .moveTo(100, yCompetences)
            .lineTo(550, yCompetences)
            .lineTo(550, yCompetences+35+{{items.length}}*20)
            .lineTo(100, yCompetences+35+{{items.length}}*20)
            .lineTo(100,yCompetences)
            .lineWidth(1)
            .strokeColor('silver')
            .stroke();

    //All competences titles
    doc.fontSize(11)
            .fillColor('grey')
            .text('COMPETENCE', 110, yCompetences+10);
    doc.fontSize(11)
            .fillColor('grey')
            .text('LEVEL', 271, yCompetences+10);
    doc.fontSize(11)
            .fillColor('grey')
            .text('SCORE', 430, yCompetences+10);
    doc.fontSize(11)
            .fillColor('grey')
            .text('#RATINGS', 490, yCompetences+10);

</script>

        </br>
    <article class="container-survey">

        <div class="row-heading">
            <div class="col-md-4">COMPETENCE</div>
            <div class="col-md-4">LEVEL</div>
            <div class="col-md-2"><div align="center">SCORE</div> </div>
            <div class="col-md-2"><div align="center">#RATINGS</div></div>
        </div>
    {{#each items}}
        <div class="row">
            <div class="col-md-4">{{[0]}}</div>
            <div class="col-md-4"><div id="meter{{@index}}"></div></div>
            <div class="col-md-2"><div align="center">{{[2]}}</div></div>
            <div class="col-md-2"><div align="center">{{[3]}}</div></div>
        </div>

        <script>
            $('#meter{{@index}}').jQMeter({
                goal: '100',
                raised: '{{[1]}}'
            });

        </script>

<script>

    //** ALL COMPETENCES **//

    //Text for each competence
    doc.fontSize(10)
            .fillColor('grey')
            .text('{{[0]}}', 110, yCompetences+40+{{@index}}*20);

    //Grey bar
    doc.lineCap('round')
            .moveTo(275, yCompetences+44+{{@index}}*20)
            .lineTo(375, yCompetences+44+{{@index}}*20)
            .lineWidth(7)
            .fillAndStroke("#d7d7d7")
            .stroke();

    //Green bar
    doc.lineCap('round')
            .moveTo(275, yCompetences+44+{{@index}}*20)
            .lineTo(275+{{[1]}}, yCompetences+44+{{@index}}*20)
            .lineWidth(7)
            .fillAndStroke("#3cb878")
            .stroke();

    //Text for average score
    doc.fontSize(10)
            .fillColor('grey')
            .text('{{[2]}}', 443, yCompetences+40+{{@index}}*20);

    //Text for #ratings
    doc.fontSize(10)
            .fillColor('grey')
            .text('{{[3]}}', 515, yCompetences+40+{{@index}}*20);

</script>

    {{/each}}

    </article>

        </br>
        <article class="container-survey">

            <div class="row-heading">
                <div class="col-md-4">EVALUER'S NAME</div>
                <div class="col-md-4">JOB TITLE</div>
                <div class="col-md-4">COMPANY</div>
            </div>

            {{#each evaluers}}

                <div class="row">
                    <div class="col-md-4">{{first_name}} {{last_name}}</div>
                    <div class="col-md-4">{{job_title}}</div>
                    <div class="col-md-4">{{company}}</div>
                </div>


            {{/each}}
    </article>

</div>

<script>

    doc.fontSize(10)
            .fillColor('blue')
            .text('WWW.YIES.CO', 100, yFooter, {align : 'center'})
            .link(300, yFooter, 70, 20, 'http://www.yies.co/');

    doc.fontSize(10)
            .fillColor('grey')
            .text('2 / 2', 530, yFooter);

    doc.end();

    stream.on('finish', function(){
        blob = stream.toBlob('application/pdf')
    });

    $(document).ready(function () {
        $('body').on('click', '#export-button', function () {
            console.log("Try to print");
            printPDF();

        });
    });

{{#each info}}

    function printPDF() {
        download( blob, "YIES report - {{[1]}} {{[2]}}", "pdf");
    }

{{/each}}

</script>

